= form_for [@post, Comment.new], remote: :true do |f|
  .form-group
    = f.text_area :body, placeholder: 'Add a comment', rows: 2, class: 'form-control'
  .form-group
    = f.submit 'Save', class: 'btn btn-sm btn-success'
